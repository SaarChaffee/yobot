version: '3'
services:
  bot:
    build: .
    container_name: skadi
    restart: on-failure
    command: bash -c "
      python3 main.py > /dev/null 2 > /dev/null
      && sh yobotg.sh
      "
    volumes:
    - ./src/client:/code